# Check — 20260920-readme-test-count

Step 5 of the intake workflow, run on 2026-09-20 at HEAD `4a0c94b`, from `/home/smoke01/dev/ancient-games`. The
verifier did not see how the design was made; it read the files in this run folder, the templates and the structured
data handed to it, and ran read-only commands. `README.md` was not touched. The full pytest suite was not run; the
only pytest calls were `--version` and `--collect-only -q -p no:cacheprovider` with `PYTHONDONTWRITEBYTECODE=1`.

One line per item.

- V1: pass — `ls` shows `runs/20260920-readme-test-count/workflow-design.md` (19003 bytes); `grep -n '^#'` on it and on `docs/workflow-templates/workflow-design.md`: all 8 template `##`/`#` headings are present with the same text and in the same order, plus one `###` block per piece (p1–p4); each of the 12 bold template fields (`Steps` … `Tools and skills`) occurs 4 times; the bullets of Predictability, Debuggability and Quality control and the parallel-candidates table header are present. One extra heading, `## Assumptions and conflicts surfaced`, is not in the template.
- V2: pass — a scratch script compared every string of the structured data (piece names, 11 steps, 4 checks, feedback, exits, files touched, briefs, 3 joins, 3 questions with assumptions, cost, success criteria) with the design after normalising `RUN/` → `runs/20260920-readme-test-count/`, backticks, whitespace, `<TAB>` and `…`: 42 strings identical, 9 not; the 9 were read side by side and differ in wording only: "the design" / "this design" (p1 withheld, p4 step 2, p4 given); the sentence "the check script file does not exist at design time … commands exist today" closes the p2 and p3 checks in the data and stands once in the Pieces preamble of the design (lines 32-34); p4 given says "(created by p2)" without "it does not exist at design time"; joins differ in `-` / `—` and bold markup; cost: "one per piece …" and "at most 8 …" sit in the "Agents in total" bullet, `NOT`/`not`, `section 2`/`§2`. With those variants substituted all 9 match. Read by `grep`: pattern, resolved spot, attempt limit, needs and check kind of p1–p4 equal the data; order line is `p1 → p2 → p3 → p4`; parallel candidates: none; agents: 4.
- V3: pass — script checks are p1, p2, p3. `command -v` found bash, python3, env, git, mktemp, sed, grep, sha256sum, tail; `python3 -m pytest --version` → `pytest 9.0.2`; dry run `python3 -m pytest --collect-only -q -p no:cacheprovider | tail -1` → `379 tests collected in 0.08s` (an N can be read from it); `sed -n '33p' README.md` → `python3 -m pytest -q          # 68 passed, 0 xfailed`; `grep -c "68 passed" README.md` → 1 (line 33 only); `sed '33s/68 passed/999 passed/' README.md` to stdout changes line 33 as the p2 check expects; `git diff --numstat` is empty today; `git status --short tests/ ancient_games/ pyproject.toml` → only `?? tests/workflows/`; `ls` shows `README.md`, `state.md`, `tests`, `ancient_games`, `pyproject.toml`. Exception, stated in the design and in the data: `runs/20260920-readme-test-count/check_readme_count.sh` does not exist (`ls`: No such file); p2 creates it before its own check runs, and p3's check runs after p2. Not tried: the full-suite commands (`NO_COLOR=1 …`, `env -u NO_COLOR … pytest -q`), only that `env`, `python3` and pytest exist.
- V4: pass — read `state.md`: `grep -c -x -F` finds the challenge line exactly once, identical to the challenge given to this step; the baseline block is present with HEAD `4a0c94b`; steps 1–4 are `done` with `readiness.md`, `algorithm.md`, `(recorded below)`, `workflow-design.md`, and the three files exist (`ls`); size decision `design — steps: 6, unclear spots: 5, risk: low`; unclear spots u1–u5 are listed, the same ids as `algorithm.md` lines 44-48, with u2 and u3 the spots p3 and p1 resolve and u1, u4, u5 the three questions; the three question rows (spot, question, assumption) are string-identical to the data (same scratch script); log lines: step 1: 1, step 2: 2 (done, redone after a failed check), step 3: 1, step 4: 1.
- V5: pass — `ls`: `PROJECT_OVERVIEW.md` (425 lines; lines 402-410 hold `378 passed, 1 failed` and `env -u NO_COLOR … # 379 passed`), `README.md`, `pyproject.toml` (`testpaths = ["tests"]` at line 12), `state.md`, `workflow-design.md`, `readiness.md` (`## Information` at line 34), `algorithm.md`, `docs/WORKFLOW_DESIGN_METHOD.md` all exist. There is no prompt file in this run, so no "what you need" section (`grep -rni "what you need"` in the run folder: no hit). Exception, stated in the brief itself: p4's brief gives `check_readme_count.sh`, which does not exist now; p4 needs p2, which creates it. The other absent files named in briefs are in the withheld lists and are outputs of the designed run (`p2-notes.md`, `p3-notes.md`, `p3-attempt-<n>.txt`).
- V6: pass — `git status --short` now, with `?? runs/` removed, is identical (`diff`) to the baseline block in `state.md`; the only added line is `?? runs/`; HEAD is still `4a0c94b`. Limit of this check: git shows untracked directories as one line, so a change inside an already untracked path of the baseline (for example `.claude/`) would not show.

Result: 6 of 6 pass. The intake script's own checks on the design are not visible to this step and were not rerun here.
