[project]
name = "ancient-games"
version = "0.1.0"
description = "The Ancient Games v1 — program-side spine of an agent-orchestration framework (V3_3_SPEC)"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
test = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
